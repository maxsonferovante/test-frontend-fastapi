[project]
name = "test-frontend-fastapi"
version = "0.1.0"
description = "Workspace for the certificate search full-stack application"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
